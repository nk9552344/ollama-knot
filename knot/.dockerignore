# Build artifacts and dependencies
node_modules
.next
out
dist

# Local data — mounted as a volume at runtime
data
!data/.gitkeep

# Env files (never bake secrets into the image)
.env
.env.local
.env.*.local

# Git / VCS
.git
.gitignore
.gitattributes

# Editor / OS / IDE noise
.vscode
.idea
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Project docs / agent metadata not needed in the image
README.md
AGENTS.md
CLAUDE.md
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.*.yml
